// export const myApplicationsPromise = (email, accessToken) => {
//   return fetch(`https://career-code-server-sigma.vercel.app?email=${email}`, {
//     headers: { authorization: `Bearer ${accessToken}` },
//   }).then((res) => res.json());
// };
